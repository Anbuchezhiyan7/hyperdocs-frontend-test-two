/**
 * Onboarding sample data re-exported from public/data.
 * This module lives inside src/ so it can be imported via the @/ alias.
 * The data is static TypeScript constants — not served as public files.
 */
export { sampleBlogData, enhancedBlogData } from '../../public/data/sampleBlogData';
